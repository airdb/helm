DESCRIPTION:
	mina-api is the backend for baobeihuijia.com.
	The QQ robot use it.

USAGE:
	helm install mina-api airdb/mina-api --set service.gdbc="root:passwd@tcp(localhost:3306)/test"
	helm install mina-api airdb/mina-api --set service.write_gdbc="root:passwd@tcp(localhost:3306)/test" --set service.gdbc="root:passwd@tcp(localhost:3306)/test"

	helm get notes  mina-api
